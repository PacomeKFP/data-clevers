part of profile;

class EducationProfile extends StatelessWidget {
  const EducationProfile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Education'),);
  }
}